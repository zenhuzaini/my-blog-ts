const $content = "content";
const $category = "category";

export const STRAPI_API = {
	CONTENT: {
		CONTENTS: `${$content}s`,
		CONTENT: `${$content}`,
		PUPULATE_ALL: "*",
	},
	CATEGORY: {
		CATEGORIES: `${$category}s`,
		CATEGORY: `${$category}`,
	},
};

function urlChange() {
	//if no parameters provided then populate all
}
